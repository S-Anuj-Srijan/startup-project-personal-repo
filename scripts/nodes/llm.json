{
  "id": "llm",
  "label": "LLM Prompt",
  "description": "Call an LLM with a prompt and transform input into structured output.",
  "inputs": [{ "id": "in", "name": "Input" }],
  "outputs": [{ "id": "out", "name": "Output" }],
  "params": [
    { "id": "apiKey", "label": "API Key", "type": "password", "required": true },
    { "id": "model", "label": "Model", "type": "string", "default": "gpt-4o-mini" },
    { "id": "prompt", "label": "Prompt", "type": "textarea", "default": "Summarize the input." }
  ],
  "script": "scripts/nodes/llm.py"
}
